# ─── Database (PostgreSQL) ───
# Local (Docker Compose)
DATABASE_URL=postgresql://pto:pto_secret@postgres:5432/pto_db?schema=public

# Vercel/Neon (production) - pooled connection
# DATABASE_URL=postgresql://user:pass@ep-xxx.us-east-2.aws.neon.tech/pto_db?sslmode=require&pgbouncer=true
# DIRECT_URL=postgresql://user:pass@ep-xxx.us-east-2.aws.neon.tech/pto_db?sslmode=require

# ─── Auth ───
JWT_SECRET=change-me-in-production-use-random-32-chars

# ─── File Storage (S3-compatible: MinIO / AWS S3 / Cloudflare R2) ───
MINIO_ENDPOINT=minio
MINIO_PORT=9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=pto-files
MINIO_USE_SSL=false

# ─── Server ───
BACKEND_PORT=3000
NODE_ENV=development
