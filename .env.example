# ─── Database (PostgreSQL) ───
# Local (Docker Compose)
DATABASE_URL=postgresql://pto:pto_secret@postgres:5432/pto_db?schema=public

# Production (direct PostgreSQL on remote server)
# DATABASE_URL=postgresql://pto_user:PASSWORD@SERVER_IP:5432/pto_db?schema=public

# ─── Auth ───
JWT_SECRET=change-me-in-production-use-random-32-chars

# ─── File Storage ───
# Type: "local" (server disk - free!) or "minio" (S3-compatible)
STORAGE_TYPE=local
STORAGE_LOCAL_PATH=./uploads

# MinIO settings (only needed if STORAGE_TYPE=minio)
# MINIO_ENDPOINT=minio
# MINIO_PORT=9000
# MINIO_ACCESS_KEY=minioadmin
# MINIO_SECRET_KEY=minioadmin
# MINIO_BUCKET=pto-files
# MINIO_USE_SSL=false

# ─── Server ───
BACKEND_PORT=3000
NODE_ENV=development
