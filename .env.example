# ─── Database (PostgreSQL via SSH tunnel) ───
# Local (Docker Compose)
DATABASE_URL=postgresql://pto:pto_secret@postgres:5432/pto_db?schema=public

# Production (SSH tunnel - PostgreSQL on remote server)
# 1. Set up SSH tunnel: ssh -L 5432:localhost:5432 user@your-server-ip
# 2. Use localhost connection through the tunnel:
# DATABASE_URL=postgresql://user:pass@localhost:5432/pto_db?schema=public

# ─── Auth ───
JWT_SECRET=change-me-in-production-use-random-32-chars

# ─── File Storage (S3-compatible: MinIO / AWS S3 / Cloudflare R2) ───
MINIO_ENDPOINT=minio
MINIO_PORT=9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=pto-files
MINIO_USE_SSL=false

# ─── Server ───
BACKEND_PORT=3000
NODE_ENV=development
